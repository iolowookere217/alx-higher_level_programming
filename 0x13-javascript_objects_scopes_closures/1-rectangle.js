#!/usr/bin/node

// a class rectangle that defines a rectangle with two argument w and h

class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}
module.exports = Rectangle;
